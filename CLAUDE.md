# pirata

Personal Mac-based media workspace: torrent search/queue (`torrentclaw` MCP + Rust `pirata` CLI) → aria2c download via `scripts/queue.py` → opportunistic contact-sheet sweeper → RAG-multimodal KB export.

## Memory

**Last snapshot:** MEMORY_DEEP_004.md
