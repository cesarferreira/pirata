# Prompt — primeiro ingest da pirata kb/ no knowledge-hub (v2 ready)

**Criado:** 2026-04-25
**Para colar em:** agente que mantém o `knowledge-hub` MCP no Dante
**Idioma:** PT-BR (peer-to-peer com o agente)
**Pré-requisito recomendado:** rodar antes o prompt de validação (cole o relatório no slot `<resultado-validacao>` abaixo). Se pular, este prompt aborta cedo com `ABORT-VALIDATION`.

**Histórico:**
- v1 em `/tmp/donna_ingest_prompt_v1.md` — assumia kwargs em `ingest_sync()` que não existem. NÃO USAR.
- v2 (este) — após review por 4 personas (adversarial / coherence / feasibility / scope-guardian) com leitura do source code real do kh em `/Users/vidigal/projects/knowledge-hub/`.

**Status final canônicos** (definidos no próprio prompt em `<status-canonicos>`): `SUCCESS`, `SUCCESS-COM-RESSALVAS`, `ABORT-PREFLIGHT`, `ABORT-VALIDATION`, `FAILED-INGEST`, `FAILED-SMOKE`.

---

```
<papel>
Você é o agente do knowledge-hub MCP no Dante. ULTRATHINK em qualquer
etapa que mude estado de FS ou do kh. Operação destrutiva pede dois
pensamentos.
</papel>

<contexto>
Vidigal quer fazer o primeiro ingest da pirata `kb/` no kh. Esta
requisição vem do Claude Code da pirata em
`/Users/vidigal/claude-code/pirata`.

Fatos verificados (cite as fontes se duvidar):
- `ingest_sync()` tem assinatura ZERO-arg
  (`/Users/vidigal/projects/knowledge-hub/src/knowledge_hub/mcp_server.py`).
  Não aceita `path`, `kb_slug`, glob filters, license metadata.
- A descoberta de KBs acontece sob `settings.public_bridge_root`,
  hoje `/Users/vidigal/knowledge-base/`. Para ingerir um diretório
  fora dessa raiz, ele precisa ser STAGED ali primeiro (symlink ou
  cópia) seguindo a convenção em
  `/Users/vidigal/projects/knowledge-hub/src/knowledge_hub/kb_discovery.py`.
- Não existe MCP tool de delete-KB exposta. `delete_kb` é interno
  (`runtime.py`) e disparado pelo catalog quando uma KB some do FS.
- `retrieve` recebe `(query, kb_slugs, mode, explain_retrieval, ...)`.
  Sem `kb_slugs` o smoke vai colidir com as outras 32 KBs.
- pirata kb/ NÃO está registrada hoje (verificado em
  `MEMORY_DEEP_003.md`). Watched roots todos sob `~/knowledge-base/`.
- SessionStart desta sessão pirata reportou degradation:
  `[knowledge-hub] status=? profile=? · retrieval may be reduced`.

Estado de pirata kb/:
- 1 filme populado: Who Framed Roger Rabbit 1988
  (`kb/per-movie/who-framed-roger-rabbit-1988.json` + frames JPG +
  contact-sheets JPG + ~1 linha em `kb/manifest.jsonl`)
- O JSON predates Unit 3 (KB enrichment), então NÃO tem campos IMDb
  (tconst, rating, top_cast, akas, genres). Valor deste ingest é
  TESTE DE PIPELINE, não recall semântico — registre isso.

Plano completo:
`/Users/vidigal/claude-code/pirata/docs/plans/2026-04-24-004-feat-imdb-local-pirata-coupling-plan.md`
(Unit 6 op-step descreve este ingest como pós-Unit-3; estamos
testando o caminho cedo a propósito.)
</contexto>

<resultado-validacao>
[VIDIGAL: cole aqui o relatório de GO/NO-GO + parametrização (slug
sugerido, exclusões, license stance) que o agente do kh produziu na
rodada de validação. Se ainda não rodou validação, escreva UNKNOWN.
Se NÃO-GO, cole o motivo. Não deixe vazio.]
</resultado-validacao>

<status-canonicos>
Use exatamente um destes 6 rótulos no Status Final do relatório:
- SUCCESS                  — ingest ok + ≥2 das 3 smoke queries com hit
- SUCCESS-COM-RESSALVAS    — ingest ok mas com gap declarado (license,
                             chunking subótimo, smoke parcial 1/3)
- ABORT-PREFLIGHT          — kh degraded, slug collision, ou convenção
                             de staging não-determinística
- ABORT-VALIDATION         — `<resultado-validacao>` UNKNOWN, vazio,
                             malformado, contraditório, ou NÃO-GO
- FAILED-INGEST            — `ingest_sync()` errou ou completou parcial
- FAILED-SMOKE             — todas as 3 queries retornaram 0 hits ou
                             crasharam após ingest aparentemente ok
</status-canonicos>

<tarefa>
Execute em ordem. ULTRATHINK antes de cada step que muda estado.

1. **Preflight (zero side-effect)**
   a. Rode `health` + `list_kbs` + `topology` frescos. Se qualquer
      componente core (vector store, reranker, ingest worker) está
      degraded → status=ABORT-PREFLIGHT, pare e reporte.
   b. Confirme que NENHUMA KB existente colide com o slug que vai usar
      (ex: `pirata-kb` ou o que `<resultado-validacao>` ditou). Se
      houver colisão → ABORT-PREFLIGHT.
   c. Confirme que a convenção de staging em `kb_discovery.py` é
      determinística para o layout pirata kb/ (per-movie JSONs +
      manifest.jsonl + dirs de assets). Se a convenção exigir um
      manifest top-level que pirata não fornece, ABORT-PREFLIGHT e
      proponha o manifest mínimo no relatório.
   d. Leia `<resultado-validacao>`:
      - GO ou GO-COM-RESSALVAS → extraia slug + exclusões e siga.
      - NÃO-GO, UNKNOWN, vazio, malformado, ou contraditório
        → status=ABORT-VALIDATION. NÃO faça inline re-validation.
        Peça que Vidigal cole resultado válido e re-dispare.

2. **Staging (modifica FS — anuncie e execute neste turno)**
   - Crie `~/knowledge-base/<slug>/` como SYMLINK para
     `/Users/vidigal/claude-code/pirata/kb/`. Comando exato:
     `ln -s /Users/vidigal/claude-code/pirata/kb /Users/vidigal/knowledge-base/<slug>`
   - Se a convenção do kh exigir cópia em vez de symlink (a confirmar
     em 1c), use `cp -R` e anote no relatório o trade-off (re-ingest
     futuro precisa re-copiar).
   - Cole o comando exato que rodou + ls/stat de verificação.

3. **Ingest** (chamada zero-arg)
   - Dispare `mcp__knowledge-hub__ingest_sync()`. SEM kwargs.
   - Capture: wallclock, número de KBs descobertas antes/depois,
     número de chunks gerados na nova KB, warnings, errors.
   - Se errou ou completou parcial → status=FAILED-INGEST. NÃO faça
     retry automático. Vá para step 5 (rollback dry-run + reporte).

4. **Smoke retrieve** (queries literais com escopo)
   Rode estas 3 chamadas exatas e cole resultados literais:
   a. `mcp__knowledge-hub__retrieve(query="Roger Rabbit", kb_slugs=["<slug>"], mode="auto", explain_retrieval=True)`
   b. `mcp__knowledge-hub__retrieve(query="1988 noir cartoon detective", kb_slugs=["<slug>"], mode="auto", explain_retrieval=True)`
   c. `mcp__knowledge-hub__retrieve(query="Bob Hoskins", kb_slugs=["<slug>"], mode="auto", explain_retrieval=True)`
   Para cada: hit_count, top-3 (path + score), wallclock,
   `degraded_components` do `explain`. Se ≥1 query crashou OU as 3
   retornaram hit_count=0 → status=FAILED-SMOKE + execute rollback.

5. **Rollback procedure** (documente; execute SOMENTE em FAILED-*)
   Procedure real (não inventada):
   - `rm /Users/vidigal/knowledge-base/<slug>` (unlink do symlink) ou
     `rm -rf` se for cópia.
   - `mcp__knowledge-hub__ingest_sync()` para o catalog reconciliar e
     disparar `delete_kb` interno.
   - Verificar: `mcp__knowledge-hub__list_kbs()` não lista `<slug>`.
   Em qualquer FAILED-*, execute esses 3 passos e cole evidência.
   Em SUCCESS / SUCCESS-COM-RESSALVAS, apenas DOCUMENTE — não execute.
</tarefa>

<entregaveis>
A. **Relatório técnico** em markdown PT-BR:
   1. Preflight findings (health/list_kbs/topology output + decisão
      sobre `<resultado-validacao>` + check de slug collision + check
      de convenção de staging)
   2. Staging executado: comando exato + verificação ls/stat
   3. Ingest executado: comando + métricas + warnings/errors
   4. Smoke retrieve: tabela query × hit_count × top-3 × wallclock ×
      degraded_components
   5. Status final (um dos 6 rótulos canônicos) + 1-2 frases de
      justificativa ligadas a evidência das seções 1-4
   6. Rollback: procedure documentada + (se executada) evidência

B. **Próximas ações para o pirata-side** (lista de até 5 bullets,
   texto plano):
   - Cada bullet = 1 ação concreta (ex: "rodar Unit 3 antes de
     re-ingest", "criar wrapper markdown em kb/per-movie/", "abrir
     issue no kh para license metadata field")
   - Sem critério de aceite formatado — v1 do ingest, simples basta
</entregaveis>

<restricoes>
- NÃO chame `workspace_sync`, `delete_kb` (interno), nem invente
  kwargs em `ingest_sync()`. Se acha que precisa de uma capability
  ausente, ABORTE e proponha como gap em "Próximas ações".
- NÃO escreva nada dentro de `/Users/vidigal/claude-code/pirata/kb/`.
  Apenas leia. O pirata-side é dono daquele path.
- NÃO faça retry automático em qualquer FAILED-*. Reporte e pare.
- NÃO infira intenção de `<resultado-validacao>` malformado.
  ABORT-VALIDATION é a resposta correta.
- Sem emojis. Sem floreio. Tom técnico, peer-to-peer.
</restricoes>

<verifique-antes-de-finalizar>
- [ ] Status final é um dos 6 rótulos canônicos
- [ ] Comando exato de staging + ingest_sync() colado e reproduzível
- [ ] As 3 queries de smoke retrieve foram chamadas LITERALMENTE
      (com `kb_slugs` e `explain_retrieval=True`) e resultados
      literais colados
- [ ] Em FAILED-*, rollback foi executado e a evidência (`list_kbs`
      sem o slug) está colada
</verifique-antes-de-finalizar>
```
